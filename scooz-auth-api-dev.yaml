server:
  port: 10000
  servlet:
    context-path: /api/v1

spring:
  application:
    name: scooz-auth-api
  data:
    mongodb:
      uri: mongodb+srv://admin:admin@cluster0.regfb.mongodb.net/scooz_authentication
      database: scooz_authentication
      auto-index-creation: true

logging:
  file: /tmp/logs/${spring.application.name}.log